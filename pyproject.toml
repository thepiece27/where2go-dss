[tool.ruff]
target-version = "py313"
extend-exclude = ["artifacts", "web/vendor"]

[tool.ruff.lint]
# Keep this cleanup focused on unused imports/locals and undefined names.
select = ["F401", "F811", "F821", "F841"]
